<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}"> {{ __('') }} <meta name="viewport" content="width=device-width, initial-scale=1"> {{ __('') }} </title> {{ __('') }} <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" />

        <!-- Scripts --> {{ __('') }} <div class="flex min-h-screen"> {{ __('') }} <div class="flex justify-center"> {{ __('') }} <svg class="h-4 text-zinc-800 dark:text-white" viewBox="0 0 18 13" fill="none" xmlns="http://www.w3.org/2000/svg"> {{ __('') }} </line>
                                        <line x1="5" y1="1" x2="5" y2="8" stroke="currentColor" stroke-width="2" stroke-linecap="round"> {{ __('') }} </line>
                                        <line x1="13" y1="1" x2="13" y2="12" stroke="currentColor" stroke-width="2" stroke-linecap="round"> {{ __('') }} </line> {{ __('') }} </div>
                            <span class="text-lg font-semibold text-zinc-800 dark:text-white"> {{ __('') }} </div> {{ __('') }} <!-- Illustration Panel --> {{ __('') }} <div class="flex gap-2 mb-4"> {{ __('') }} <flux:icon icon="star" variant="solid" class="text-yellow-400" /> {{ __('') }} </div>

                    <div class="mb-6 italic font-base text-2xl xl:text-3xl text-white">
                        {{ __('Professional WCAG accessibility auditing and reporting tools.') }}
                    </div> {{ __('') }} <div class="flex flex-col justify-center font-medium">
                            <div class="text-lg text-white">{{ config('app.name') }}</div>
                            <div class="text-zinc-300">{{ __('Accessibility Made Easy') }}</div> {{ __('') }} </div> {{ __('') }} </body>
</html>
